@keyframes slideUp
  to
    transform: translateY(-50px)
    opacity: 0

@keyframes slideDown
  to
    transform: translateY(50px)
    opacity: 0
    
@keyframes spin
  from 
    transform: rotate(0deg)
  to
    transform: rotate(360deg)