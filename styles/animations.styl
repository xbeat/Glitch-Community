@keyframes slideUp
  to
    transform: translateY(-50px)
    opacity: 0

@keyframes slideDown
  to
    transform: translateY(50px)
    opacity: 0
    
@keyframes spin
  50% 
    transform: rotate(360deg)
  100%
    transform: rotate(0deg)