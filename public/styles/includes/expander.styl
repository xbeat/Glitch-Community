.expander
  transition: max-height 500ms ease-out // sync with the setTimeout
  position: relative
  overflow: hidden
  
  .expander-bottom
    background: linear-gradient(to bottom, transparent, white)
    position: absolute
    min-height: 25px
    bottom: 0
    left: 0
    right: 0
    pointer-events: none
    > *
      pointer-events: initial